snapshots:
  - name: account_snapshot
    relation: ref('stg_account')
    config:
      database: banking
      unique_key: account_id
      strategy: check
      check_cols:
        - status        # bao gồm cả giá trị CLOSED — không cần cột is_deleted riêng
        - account_type
        - balance